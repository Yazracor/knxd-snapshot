# Automatically added by dh_installinit
if [ -x "/etc/init.d/knxd" ]; then
	update-rc.d knxd defaults >/dev/null
	invoke-rc.d knxd start || exit $?
fi
# End automatically added section
# Automatically added by dh_makeshlibs
if [ "$1" = "configure" ]; then
	ldconfig
fi
# End automatically added section
